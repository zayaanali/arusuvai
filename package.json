{
  "name": "pantry-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Simple pantry manager website with Express + SQLite",
  "main": "server/index.js",
  "scripts": {
    "dev": "PORT=${PORT:-18001} HOST=${HOST:-0.0.0.0} nodemon --watch server --watch public --watch app/frontend --ext js,json,html,css server/index.js",
    "start": "node server/index.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "openai": "^6.5.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
